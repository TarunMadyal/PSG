gradle-wrapper.jar
/.gradle
/captures/
/gradlew
/gradlew.bat
/local.properties
GeneratedPluginRegistrant.java
.cxx/

# Note: for this single-shop, sideloaded app we intentionally commit a stable
# signing keystore + key.properties so every build (local and CI) is signed
# identically and the app updates in place. This is a deliberate trade-off for a
# private, non-Play-Store app.
